# ─── Frontend Service ─────────────────────────────────────────────────────
# ClusterIP - Ingress will route traffic to it
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: cloudshadow
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80